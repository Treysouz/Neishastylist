import SectionWrapper from "./section-wrapper";

export default function ServicesSection() {
  return (
    <SectionWrapper id="services" className="h-dvh">
      services
    </SectionWrapper>
  );
}
